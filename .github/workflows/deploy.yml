name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build TypeScript
        run: yarn build

      - name: Create executable package
        run: |
          chmod +x build-executable.sh
          ./build-executable.sh
          ls -la
          ls -la fos-printer-app/ || echo "fos-printer-app directory not found"

      - name: Prepare files for deployment
        run: |
          mkdir -p public
          ls -la
          cp -r fos-printer-app public/ || echo "Failed to copy fos-printer-app"
          cp fos-printer-app.zip public/ || echo "Failed to copy zip file"
          ls -la public/
          # The index.html is already in the public directory from the repository

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
